<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>MyWebApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
  </head>

  <body>
    <header class="p-3 bg-dark text-white">
      <div class="container">
        <nav class="d-flex flex-wrap align-item-center justify-content-center justify-content-lg-start">
          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">  
            <li><%= link_to 'Home', root_path, class: 'nav-link px-2 text-white'%></li> <%# переход через код руби, можно через root_url тогда в строке будет полный 
                                                      домен сайта(example.com/question), а с root_path только часть(/question)%>
            <li><%= link_to 'Questions', questions_path, class: 'nav-link px-2 text-white'%></li>
          </ul>
        </nav>
      </div>
    </header>

    <main class="container mt-3">

      <% flash.each do |k,v|%>
        <%= tag.div v, class: "alert alert-#{k}", role: 'alert' %> <%# равносильно тому что мы запишем v в блоке div с классом %>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
