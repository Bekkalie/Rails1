<% provide :page_title, 'Edit profile' %> <%# метод provide может передовать что то методу вспомогательному yield(в нашем случае 
который находится в основном файле application.html.erb)%>

<% currently_at 'Edit profile' %> 

<%# добавляем возможность редактировать профиль пользователя%>

<h1 class="mb-4">Edit profile</h1>

<%= render 'form' %>